# Vercel AI Gateway
#
# RemcoChat uses Vercel AI Gateway via the Vercel AI SDK.
# The app expects the key to be present in the process environment when running via `npm run dev` / `npm run start`.
#
# VERCEL_AI_GATEWAY_API_KEY=

# OpenCode Zen (optional)
# Used when `config.toml` configures an `openai_compatible` provider that points at OpenCode.
#
# OPENCODE_API_KEY=

# Exa Search (optional)
# Used for web search when the active provider is openai_compatible.
#
# EXA_API_KEY=

# Stadia Maps (optional)
# Used for static map thumbnails in the weather tool cards.
# If set, `/api/static-map` will use Stadia's static maps.
# If not set, `/api/static-map` falls back to stitching OpenStreetMap tiles from https://tile.openstreetmap.org.
#
# STADIA_MAPS_API_KEY=
#
# Legacy/alternative (client-visible) key name. Prefer the server-only var above.
# NEXT_PUBLIC_STADIA_MAPS_API_KEY=

# Admin tools (optional)
# Enables /api/admin/export + /api/admin/reset and shows an Admin button in the UI.
# REMCOCHAT_ENABLE_ADMIN=1

# Bash tools via Vercel Sandbox (optional; see config.toml.example)
# REMCOCHAT_ENABLE_BASH_TOOL=1
#
# Vercel Sandbox auth (preferred: short-lived OIDC token via `vercel env pull`)
# VERCEL_OIDC_TOKEN=
#
# Alternative Vercel Sandbox auth
# VERCEL_TOKEN=
# VERCEL_ORG_ID=
# VERCEL_PROJECT_ID=
#
# Required only when app.bash_tools.access="lan"
# REMCOCHAT_ADMIN_TOKEN=

# modelsdev (required)
# For Docker deployments, RemcoChat expects the host to provide the modelsdev install directory
# (must contain `bin/modelsdev`) and mounts it into the container.
# REMCOCHAT_MODELSDEV_CLI_HOST_DIR=/usr/local/lib/modelsdev
