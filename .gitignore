# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results
/playwright-report

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.key

# nginx proxy certs (local only; never commit)
/nginx/certs/*
!/nginx/certs/.gitkeep
/nginx/ca/*
!/nginx/ca/.gitkeep

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (commit only templates)
.env*

# Optional Docker deployment config (mounted via REMCOCHAT_CONFIG_TOML).
/config.docker.toml
!.env.example

# local data
/data/

# runtime config (commit only templates/examples)
config.toml

# local tracking lists (kept local)
FEATURES.toml
ISSUES.toml
PROGRESS.toml

# vercel
.vercel

# letsencrypt
/letsencrypt/
letsencrypt-renew.log

# typescript
*.tsbuildinfo
next-env.d.ts
2026-01-13-RemcoChat.code-workspace

# bun
bun.lock
bun.lockb
